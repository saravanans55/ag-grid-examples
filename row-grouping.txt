function formatNumberWithCommas(value) {
  // Convert the value to a string and remove any decimal places
  const formattedValue = String(value).split('.')[0];

  // Apply comma separation for thousands
  return formattedValue.replace(/\B(?=(\d{3})+(?!\d))/g, ',');
}
