data.forEach((item) => {
  if (item.cellClass === "date") {
    dateFieldsArray.push(item.headerName);
  }

  if (item.children) {
    item.children
      .filter((child) => child.cellClass === "date")
      .forEach((child) => dateFieldsArray.push(child.field));
  }
});
