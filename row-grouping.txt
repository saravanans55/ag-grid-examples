// Assuming you have the object and the array of objects
const mainObject = {
  id: 1,
  name: "John",
  age: 30,
  // Add other fields as needed
};

const arrayOfObjects = [
  { id: 2, name: "Alice", age: 25 /* Add other fields as needed */ },
  { id: 1, name: "John", age: 30 /* Add other fields as needed */ },
  // Add more objects to the array (total 10,000 records)
];

// Check if the mainObject matches any object in the array
const matchedObject = arrayOfObjects.find(
  (item) => JSON.stringify(item) === JSON.stringify(mainObject)
);

if (matchedObject) {
  console.log("Main object matches an object in the array.");
} else {
  console.log("Main object does not match any object in the array.");
}
