function customStatusCountAggFunction(values, params) {
  const dynamicValue = params.context.dynamicValue; // Fetch the dynamic value from the context

  const validStatusValues = arr_1.map(item => item.cspMasterTypeId); // Get the list of valid cspMasterTypeIds
  let count = 0;

  values.forEach(value => {
    if (validStatusValues.includes(value) && value === dynamicValue) {
      count++;
    }
  });

  return count;
}
