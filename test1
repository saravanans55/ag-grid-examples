<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
  <path d="M9 19l-7-7 7-7" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
</svg>


<div class="image-selection-container">
  <h2>Image manager — Image Selection for Appendix</h2>
  <p>Drag your files here or Click here to browse and upload files (Maximum of 50 files each, up to 200MB)</p>
  
  <div class="panel-container">
    <!-- Left Panel (Tasks Section with Sub-tabs) -->
    <mat-card class="tasks-panel equal-height">
      <mat-tab-group>
        <mat-tab label="Tasks">
          <div class="sub-tab-container">
            <mat-tab-group>
              <mat-tab label="216829">
                <div class="task-content">
                  <mat-form-field appearance="outline" class="search-field">
                    <mat-label>Search</mat-label>
                    <input matInput placeholder="Search">
                  </mat-form-field>
                  <div *ngIf="images.length" class="image-grid">
                    <div *ngFor="let image of images" class="image-item">
                      <img src="https://via.placeholder.com/80" alt="Image">
                      <p>{{ image }}</p>
                    </div>
                  </div>
                  <!-- Display a placeholder message when there are no images -->
                  <div *ngIf="!images.length" class="no-images-placeholder">
                    No images available.
                  </div>
                </div>
              </mat-tab>
              <!-- Other tabs -->
            </mat-tab-group>
          </div>
        </mat-tab>
      </mat-tab-group>
    </mat-card>

    <!-- Navigation Buttons (Centered Between Panels) -->
    <div class="nav-buttons">
      <button mat-icon-button class="carrot-icon">
        <img src="assets/left-carrot.png" alt="Left Carrot">
      </button>
      <button mat-icon-button class="carrot-icon">
        <img src="assets/right-carrot.png" alt="Right Carrot">
      </button>
    </div>

    <!-- Right Panel (Activities Section with Sub-tabs) -->
    <mat-card class="activities-panel equal-height">
      <mat-tab-group>
        <mat-tab label="Narrative">
          <div class="image-grid">
            <!-- Your existing code for displaying images -->
          </div>
          <div *ngIf="!images.length" class="no-images-placeholder">
            No images available.
          </div>
        </mat-tab>
        <!-- Other tabs -->
      </mat-tab-group>
    </mat-card>
  </div>
</div>
.image-selection-container {
  text-align: center;
  padding: 20px;
}

.panel-container {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
}

.tasks-panel, .activities-panel {
  width: 45%;
  padding: 15px;
}

.equal-height {
  min-height: 80vh; /* Maintain equal height as per screen */
  display: flex;
  flex-direction: column;
  justify-content: space-between;
}

.nav-buttons {
  display: flex;
  flex-direction: column;
  gap: 10px;
  align-items: center;
  justify-content: center;
}

.carrot-icon img {
  width: 24px;
  height: 24px;
}

.search-field {
  width: 100%;
  margin-top: 10px;
}

.image-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 10px;
  margin-top: 15px;
}

.no-images-placeholder {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100%;
  font-size: 18px;
  color: #888;
}

.select-all {
  display: inline-block;
  margin-top: 10px;
}
